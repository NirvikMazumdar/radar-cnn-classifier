\### Data Processing



The original RGB images are not used directly.



Instead:

\- Object bounding boxes from VOC annotations are extracted

\- A synthetic radar-like representation is generated

\- Class labels are derived using dominant object area



Note:

This radar representation is \*\*synthetic\*\* and used only to prototype the machine learning pipeline. It does not represent real radar sensor data.

